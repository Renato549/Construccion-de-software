Laboratorio desarrollado en el proyecto - https://github.com/A01610805/Proyecto_Tickets/tree/develop


    static fetchPreguntas(id) {
        return db.execute(
            'SELECT * FROM pregunta WHERE ID_categoria=?', [id]);
    }    
    static fetchAll() {
        return db.execute('SELECT ID_usuario, ID_rol, nombre, apellido_paterno, apellido_materno, correo, nombre_rol FROM usuario NATURAL JOIN rol');
    }

    static fetchAll() {
        return db.execute(
            'SELECT * FROM categoria WHERE disponible = 1');
    }

        obtenerid(){
        return db.execute('SELECT ID_categoria FROM categoria ORDER BY ID_categoria DESC LIMIT 1')
    }
